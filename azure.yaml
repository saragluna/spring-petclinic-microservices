# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: spring-petclinic-microservices
services:
  app:
    language: java
    project: ./spring-petclinic-microservices-apphost/pom.xml
    host: containerapp
